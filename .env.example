# Copy this file to .env and fill in your key — .env is gitignored, so it
# never gets committed. To rotate the key, just edit .env and restart the
# app; no code changes needed.
GROQ_API_KEY=your-groq-api-key-here
